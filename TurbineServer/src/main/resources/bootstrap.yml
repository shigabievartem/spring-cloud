server:
  port: 8901
spring:
  application:
    name: TurbineServer
  cloud:
    config:
      uri: http://localhost:8600
turbine:
  stream:
    destination: hystrixStreamOutput

eureka.client.registerWithEureka: false