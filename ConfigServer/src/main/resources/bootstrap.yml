spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shigabievartem/spring-cloud-configs.git
          skip-ssl-validation: true
server:
  port: 8600